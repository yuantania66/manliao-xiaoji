# 慢聊小记 ToDo List

## P0 上线前必须完成

- [x] 填写正式小程序 AppID：更新 `miniprogram-project/project.config.json` 的 `appid`。
- [ ] 确认小程序主体认证已完成。
- [ ] 确认服务类目与慢聊小记功能一致，避免误选医疗诊疗类目，除非已有资质。
- [ ] 确认正式 API 域名已备案、HTTPS 证书有效。
- [x] 在微信后台配置合法 `request` 域名；如启用上传、下载或 WebSocket，同步配置对应域名。
- [x] 更新 `miniprogram-project/config/api.js` 的体验版/正式版 API 地址。
- [ ] 配置生产环境变量：`DATABASE_URL`、`SESSION_SECRET`、`APP_ENV=production`、`AI_PROVIDER`、对应模型 API Key。
- [x] 腾讯云服务器部署生产应用，PM2 进程和 Nginx HTTP 入口已配置。
- [ ] 确认生产数据库日志、备份和恢复策略。
- [x] 准备审核说明：本版使用微信登录，可选择游客模式体验。

> 2026-07-02 复核：`https://manliaoxiaoji.com/api/health` 当前 DNS 解析失败；直连服务器 IP 并带 `Host: manliaoxiaoji.com` 返回 DNSPod webblock 跳转。正式域名在解除拦截并返回健康检查前，按 P0 阻塞处理。

## P0 真实服务闭环

- [x] 后端微信登录支持真实 `code2Session`，生产环境替换当前 mock openid。
- [x] 安全存放并读取微信 `AppID` / `AppSecret`，避免写入前端或提交到仓库。
- [ ] 短信验证码服务暂缓；代码已预留腾讯云短信，待企业/个体资质、短信签名和模板审核通过后再启用手机号登录。
- [ ] 配置 DeepSeek 或智谱 API Key，并验证慢慢说不再只走 mock/fallback 回复。
- [x] 新增小记图片上传接口 `POST /api/uploads/notes`。
- [x] 小程序保存小记前用 `wx.uploadFile` 上传图片。
- [x] 小程序创建小记时从 `images` 改为提交后端需要的 `mediaUrls`。
- [x] 历史页和详情页统一读取并展示 `mediaUrls`。
- [x] 上传接口补充登录校验、文件类型校验、大小限制、数量限制和随机文件名。

## P1 产品功能补齐

- [x] 意见反馈接后端接口或明确最终承接方式，例如邮箱、客服微信、工单系统。
- [x] 账号注销接真实后端：二次验证、清云端数据、清 session、返回注销结果。
- [ ] 慢聊小记观察从固定示例词改为真实聊天和小记统计。
- [ ] 观察功能保留授权开关，并补充撤回授权或清空观察数据路径。
- [ ] 评估是否需要对象存储方案，例如腾讯云 COS 或阿里云 OSS，替代服务器本地存储。

## P0 小程序 QA

- [ ] 用微信开发者工具打开 `miniprogram-project`，确认能正常编译预览。
- [x] 修复本机开发数据库账号认证后，重跑登录、小记、上传、聊天、搜索、日历接口闭环。
- [ ] iOS 真机完整测试首页、登录、聊天、小记、历史、日历、搜索、我的、设置。
- [ ] Android 真机完整测试首页、登录、聊天、小记、历史、日历、搜索、我的、设置。
- [ ] 未登录进入聊天/小记时提示清楚，不展示旧用户数据。
- [ ] 登录成功后 token 能保存，重新打开小程序能自动恢复登录。
- [ ] token 失效或后端返回 401 时，本机登录态会被清空。
- [ ] 新建、查看、删除小记全链路通过。
- [ ] 小记图片上传、预览、保存和弱网失败提示通过。
- [ ] 聊天发送、AI 回复、历史恢复、搜索定位、聊天日历跳转通过。
- [ ] 退出登录后，小记、聊天、日历和本机缓存不显示旧数据。
- [ ] 小笺保存图片：允许权限、拒绝权限、取消保存、保存成功、保存失败都提示清楚。
- [ ] 弱网或接口失败时，各页面显示可理解的错误提示。
- [ ] 不出现测试文案、假按钮、空页面、无法返回或审核人员无法进入核心功能的路径。

> 2026-07-02 自动化补充：已确认 `npm run check:launch` 通过，覆盖 lint、预发布审计、会话理解断言、Prisma、小程序 `.js` 语法检查和 Next build；`npm run smoke:local-api` 通过，覆盖匿名鉴权、验证码登录、小记 CRUD、图片上传、聊天、搜索、日历和退出登录；已移除小程序“我的”页与 Web 首页登录失败后写入 `local_demo_` 假 token 的兜底逻辑。

## 审核材料

- [x] 微信后台隐私保护指引按实际能力填写：微信登录标识、媒体选择、相册写入、反馈处理、账号注销等。
- [x] 用户协议与隐私政策最终文本确认，小程序内文案和微信后台内容保持一致。
- [x] 版本说明：慢聊小记 v2.0.0 第一版，提供情绪记录、小记、慢慢说、心情回看、设置与反馈。
- [x] 审核路径：进入首页 -> 点击“慢慢说”体验聊天 -> 返回首页 -> 点击“小记”填写内容 -> 点击“我的”查看设置、协议和隐私政策。
- [x] 登录说明：审核备注写清楚本版使用微信登录，也可使用游客模式体验基础流程。
- [x] 特殊说明：慢聊小记是日常陪伴与记录工具，不提供医疗诊断、心理治疗或危机干预服务。

## 发布后观察

- [ ] 审核通过后先小范围发布半天到一天。
- [ ] 观察登录失败率、接口错误率、AI 回复失败率、图片上传失败率。
- [ ] 收集首批反馈，优先处理无法登录、无法保存、无法返回、数据看不到这类阻断问题。
